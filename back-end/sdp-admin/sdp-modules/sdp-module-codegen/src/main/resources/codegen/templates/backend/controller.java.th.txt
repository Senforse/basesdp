package [[${rootPackage}]].controller;

import cn.dev33.satoken.annotation.SaCheckPermission;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import org.nmgyj.common.ApiResponse;
import [[${rootPackage}]].entity.[[${entityName}]];
import [[${rootPackage}]].service.[[${serviceName}]];
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

/**
 * [[${entityName}]] 管理接口。
 *
 * @author nmgyj
 * @since 2026-05-15
 */
@RestController
@RequestMapping("/api/admin/[[${basePath}]]")
@Tag(name = "[[${entityName}]]")
public class [[${controllerName}]] {

    private final [[${serviceName}]] [[${serviceVar}]];

    public [[${controllerName}]]([[${serviceName}]] [[${serviceVar}]]) {
        this.[[${serviceVar}]] = [[${serviceVar}]];
    }

    @Operation(summary = "列表")
    @GetMapping
    @SaCheckPermission("[[${permPrefix}]]:list")
    public ApiResponse<List<[[${entityName}]]>> list() {
        return ApiResponse.success([[${serviceVar}]].listAll());
    }

    @Operation(summary = "详情")
    @GetMapping("/{id}")
    @SaCheckPermission("[[${permPrefix}]]:list")
    public ApiResponse<[[${entityName}]]> detail(@PathVariable("id") [[${pkType}]] id) {
        return ApiResponse.success([[${serviceVar}]].getById(id));
    }

    @Operation(summary = "新增")
    @PostMapping
    @SaCheckPermission("[[${permPrefix}]]:add")
    public ApiResponse<Void> create(@RequestBody [[${entityName}]] body) {
        try {
            [[${serviceVar}]].save(body);
            return ApiResponse.success(null);
        } catch (IllegalArgumentException ex) {
            return ApiResponse.fail(ex.getMessage());
        }
    }

    @Operation(summary = "更新")
    @PutMapping("/{id}")
    @SaCheckPermission("[[${permPrefix}]]:update")
    public ApiResponse<Void> update(@PathVariable("id") [[${pkType}]] id, @RequestBody [[${entityName}]] body) {
        try {
            body.set[[${pkFieldUpper}]](id);
            [[${serviceVar}]].update(body);
            return ApiResponse.success(null);
        } catch (IllegalArgumentException ex) {
            return ApiResponse.fail(ex.getMessage());
        }
    }

    @Operation(summary = "删除")
    @DeleteMapping("/{id}")
    @SaCheckPermission("[[${permPrefix}]]:delete")
    public ApiResponse<Void> delete(@PathVariable("id") [[${pkType}]] id) {
        [[${serviceVar}]].delete(id);
        return ApiResponse.success(null);
    }
}
